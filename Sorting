#include<iostream>

using namespace std;

int findLargestNumIndex(int array)
    {
        int largest = array[0];
        for(int x =0; x < 0; x++)
        {
            if(largest < array[x]) largest = array[x];
        }
    }
int main (){
    int x[] = {6,1,5,2,4};
    return 0;
}